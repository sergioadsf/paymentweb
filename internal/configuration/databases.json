[
    {
        "name": "pagamento",
        "hostname": "${PAG_DB_HOSTNAME}",
        "password": "${POSTGRES_PASSWORD}",
        "username": "${POSTGRES_USER}",
        "dbname": "${POSTGRES_DB}"
    }
]